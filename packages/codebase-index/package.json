{
  "name": "@pi/codebase-index",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@pi/embeddings": "workspace:*",
    "@pi/memory-store": "workspace:*",
    "@pi/repo-map": "workspace:*"
  },
  "devDependencies": {
    "chokidar": "^3.6.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
