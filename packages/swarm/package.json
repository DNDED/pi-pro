{
  "name": "@pi/swarm",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@pi/cache": "workspace:*",
    "@pi/optimizer": "workspace:*",
    "@pi/provider": "workspace:*",
    "@pi/subagent": "workspace:*",
    "@pi/tasks": "workspace:*",
    "@pi/tools": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
