---
type: daily
date: 2026-06-12
---

# 2026-06-12

## Sessions

- **v0.8.3 ex mode UI integration** (commit 29a0202) — PromptInput renders ex mode
- **v0.8.4 design + build** (10 atomic commits) — Workflow + UI Overhaul Bundle

## v0.8.4 shipped

Sid asked: "what version is next" → explored pi.dev/packages (3,905 packages) → identified gaps → designed Workflow + UI Overhaul bundle.

Sid asked to add @llblab/pi-telegram and pi-zentui. Telegram deferred to v0.9.0; zentui-style UI built in-tree.

Sid said: "Tab cycles modes (build ↔ plan) like OpenCode" → AgentMode cycle logic.

**10 atomic commits pushed.** All 19 packages build clean. 1321 tests pass (was 1160; +161).

## Key code locations

- `packages/config/` — new package, foundation
- `packages/tui-pro/src/util/agent-mode.ts` — mode display helper
- `packages/tui-pro/src/extensions/plan-mode-utils.ts` — DESTRUCTIVE+SAFE PATTERNS
- `packages/tui-pro/src/extensions/todo-state.ts` — Todo state machine
- `packages/tui-pro/src/util/git-status.ts` — porcelain + Nerd Font/ASCII
- `packages/tui-pro/src/util/runtime-detect.ts` — 26 language markers
- `packages/tui-pro/src/util/nerd-fonts.ts` — heuristic detection
- `packages/tui-pro/src/components/StarshipFooter.tsx` — zentui footer
- `packages/tui-pro/src/components/EditorFrame.tsx` — zentui bordered editor
- `packages/subagent/src/tool-gate.ts` — DESTRUCTIVE bash patterns
- `apps/pi-pro/src/commands/{config,mode,plan,todos,ui}.ts` — CLI

## Pending (deferred)

- Tag v0.8.4 (waiting for Sid's ask)
- `--config` flag in CLI (planned, not done)
- LSP, web viewer, plugin system, full Telegram, live LLM bench

## v0.9.0 candidates (Telegram, plugins, subagents)

Sid picked Telegram (full adapter, ~128 tests) for v0.9.0. Will resume when v0.8.4 is tagged.
