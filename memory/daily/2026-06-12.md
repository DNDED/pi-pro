---
type: daily
date: 2026-06-12
---

# 2026-06-12

## pi-pro v0.2.0 work

Continued from morning session. Fixed 3 typecheck errors in `index.ts`:

1. `registerShortcut` handler signature — handler takes 1 arg (ctx), not 0. Fixed.
2. `sendUserMessage` doesn't accept `deliverAs: "nextTurn"` — only `"steer" | "followUp"`. Removed the option.
3. `todo` tool return type — TS infers the generic `T` from the first return's `details` shape, constrains all returns. Fixed by typing `details: TodoDetails` and `as TodoDetails` per return. Defined `TodoDetails` interface with all fields optional except items+nextId.
4. `clearMemory()` requires `state: MemoryState` arg — was being called with 0 args.

Verified: typecheck clean, 64 extension tests + 27 config tests = 91 total pass. `pi` loads the extension without errors (still blocked by Go API key on live runs, expected).

## Decisions

- `ctx.ui` methods (setStatus, setWidget) cast through structural typing since the published ExtensionAPI d.ts doesn't expose them, but runtime pi-mono has them. Documented in memory.
- `tab` shortcut override stays (Sid: "it should be tab"). pi-mono warns about built-in conflict but uses extension handler.

## Next

- Stage + commit v0.2.0 (Sid: "ok continue" was given but no commit ask yet)
- Tag v0.2.0
- Test in fresh terminal
