# 2026-06-11 ~03:20 — pi-pro v0.8.0 UX Differentiation build (COMPLETE)

**Trigger:** Sid: "sk-... make sure it works" / "ok build it" / "continue"

## Outcome

v0.8.0 UX Differentiation — SHIPPED in 1 turn. 3 atomic commits pushed to `DNDED/pi-pro` master. Live LLM bench deferred (key 401s on completions).

## Phase 1: Live bench (gated by key)

- **Step 1.1:** Probed OpenCode Go key `sk-qJ4wv5cn8BKlUnoblJzbKqmQbCTd2T6Ok4dmVic7lBIDOHSI0hvzC8XCzEi6Ed6I`
  - `/v1/models` → 200 OK
  - `/v1/messages` (Anthropic, all 6 models) → 401 "Missing API key"
  - `/v1/chat/completions` (OpenAI, all 4 models) → 401 "Missing API key"
- **Diagnosis:** key valid for read-side (Cloudflare worker for `/v1/models`), but completion endpoints return 401 from a different auth path. Likely Go subscription not active for this key.
- **Status:** key never written to any file (only used in `env OPENCODE_GO_API_KEY=… node -e` invocations). Not in shell history. Not in any committed file.
- **Action:** moved to Phase 2 immediately. Live bench becomes v0.7.1 follow-up gated on key activation at https://opencode.ai/auth.

## Phase 2: v0.8.0 UX Differentiation (3 tasks, 1 turn)

### Task A — Per-turn cost display in Footer (+16 tests)
- `LlmWorker.getLastTurnUsage()` returns `{tokensIn, tokensOut, costUsd, durationMs, toolCalls, turnNumber}` snapshot of last LLM call (+6 tests in subagent)
- `LlmWorker.getDeltaSinceLastRun()` returns delta of last `run()` vs previous; null until 2nd `run()` invocation
- `Footer.turnDelta` prop renders `Δtok:1.5K↗/500↘ $0.01 3🔧 1m30s` in accent color; omits cost when 0, omits tool count when 0/undefined (+10 tests in tui-pro)
- Session `totalCostUsd` now also accumulates from `chunk.usage.costUsd` (so tests without optimizer can verify)
- Bug fixed: prev* snapshot now set in try/finally (covers all 3 return paths in `run()`)

### Task B — Clickable links in StreamingText (+17 tests)
- New `util/links.ts` with `parseLinks(text: string): LinkSegment[]`
- Detects HTTP/HTTPS URLs, `file://` URLs, and `file.ext:LINE` refs
- Dedup overlapping matches; URLs take precedence over file:line inside them
- `file:line` href to `file://$PWD/path#L<line>` for editor integration
- `StreamingText` refactored to render URL segments in cyan+underline (terminals auto-link)

### Task C — Vim motions in PromptInput (+20 tests)
- Cursor position tracking via React `useState`, auto-syncs with value changes
- Insert at cursor (not just append); backspace/delete at cursor
- Vim-style insert-mode bindings: `h`/`l` (cursor L/R), `w`/`b`/`e` (word forward/back/end), `0`/`$` (line start/end)
- `Home`/`End` + arrow keys also work
- `Ctrl+W` (delete word back), `Ctrl+U` (kill to line start), `Ctrl+A`/`Ctrl+E` (Emacs-style line start/end)
- `Esc` (clear input)
- Inverse-color cursor (accent theme color)
- Hint footer: `esc:clear  ^w:del-word  h/l:move  w/b:word  0/$:line`
- `wordForward` / `wordBackward` exported as utility helpers

## Final state

- **Working tree:** clean
- **GitHub:** 3 commits ahead of v0.7.0-finish
- **Test count:** 1016 → 1069 (+53 tests)
- **Packages:** 18 (no new packages; v0.8.0 is feature work on existing)

## Verification

- `pnpm -r test`: 1069/1069 pass
- `pnpm -r typecheck`: clean
- `pnpm -r build`: 18 packages clean
- Live LLM bench deferred (key 401 on completions; re-run after activating Go sub at https://opencode.ai/auth)

## v0.8.0 release summary

47 new TUI tests + 6 new LlmWorker tests = 53 new. 3 atomic commits pushed.

**v0.8.0 done. v0.8.1 next:** full modal vim (esc → normal mode, d/c/y, visual mode, ex commands), sqlite-vss, web session viewer, live LLM bench attribution (key fix first).

**Vault links:** [[../projects/pi-pro]] (v0.8.0 status), [[../decisions/v0.7.0]] (9 decisions, foundation)
