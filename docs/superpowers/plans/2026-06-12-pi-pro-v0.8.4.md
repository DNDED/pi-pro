# v0.8.4 — Implementation Plan

**Date:** 2026-06-12
**Status:** Ready to execute
**Owner:** OpenCode (TDD discipline, 1 atomic commit per feature)

## Execution order (10 atomic commits)

### Commit 1: feat(config): @pi/config package
**New package:** `packages/config/`
- `package.json` (deps: zod, devDeps: typescript, vitest)
- `tsconfig.json`, `tsconfig.test.json`, `vitest.config.ts`
- `src/types.ts` (Zod schema, PiConfig, AgentMode)
- `src/paths.ts` (XDG paths, `getConfigPath()`)
- `src/defaults.ts` (DEFAULT_CONFIG)
- `src/load.ts` (loadConfig, accepts optional path)
- `src/save.ts` (atomic write, temp + rename)
- `src/validate.ts` (Zod validation, version check)
- `src/merge.ts` (precedence: defaults < file < env < cli-flag)
- `src/index.ts`
- `test/load.test.ts` (3 tests)
- `test/save.test.ts` (3 tests)
- `test/validate.test.ts` (4 tests)
- `test/merge.test.ts` (4 tests)
- `test/paths.test.ts` (3 tests)
- `test/defaults.test.ts` (2 tests)
- `test/agent-modes.test.ts` (3 tests — cycle, list, lookup)
- `test/env-merge.test.ts` (3 tests)

**Wire:** `pnpm-workspace.yaml` add `packages/config`.

**Tests:** ~25

### Commit 2: feat(tui-pro): agent mode cycle + Tab dispatch
**Files:**
- `packages/tui-pro/src/util/agent-mode.ts` (pure cycle logic, no React)
- `packages/tui-pro/src/components/PromptInput.tsx` (Tab dispatch via `onTab` prop)
- `packages/tui-pro/test/agent-mode.test.ts` (~12 tests)
- `packages/tui-pro/test/prompt-input-tab.test.tsx` (~3 tests)

**Tests:** ~12

### Commit 3: feat(subagent): LlmWorker.setActiveTools + tool gate
**Files:**
- `packages/subagent/src/llm-worker.ts` (extend with `setActiveTools` + `getActiveTools`)
- `packages/subagent/src/tool-gate.ts` (new — bash allowlist check)
- `packages/subagent/test/tool-gate.test.ts` (~6 tests)
- `packages/subagent/test/llm-worker-tools.test.ts` (~4 tests)

**Tests:** ~10

### Commit 4: feat(tui-pro): plan-mode extension
**Files:**
- `packages/tui-pro/src/extensions/plan-mode.tsx` (extension hook)
- `packages/tui-pro/src/extensions/plan-mode-utils.ts` (DESTRUCTIVE_PATTERNS, isSafeCommand, extractTodoItems, markCompletedSteps, cleanStepText)
- `packages/tui-pro/src/components/PlanModeWidget.tsx` (progress widget)
- `packages/tui-pro/test/plan-mode-utils.test.ts` (~10 tests)
- `packages/tui-pro/test/plan-mode.test.tsx` (~8 tests)

**Tests:** ~18

### Commit 5: feat(tui-pro): todo extension
**Files:**
- `packages/tui-pro/src/extensions/todo.tsx` (extension hook + TodoListComponent)
- `packages/tui-pro/src/extensions/todo-state.ts` (Todo interface, actions: list/add/toggle/clear)
- `packages/tui-pro/test/todo-state.test.ts` (~10 tests)
- `packages/tui-pro/test/todo.test.tsx` (~5 tests)

**Tests:** ~15

### Commit 6: feat(tui-pro): git status parser
**Files:**
- `packages/tui-pro/src/util/git-status.ts` (parsePorcelain, formatIcons, ASCII fallback)
- `packages/tui-pro/test/git-status.test.ts` (~8 tests)

**Tests:** ~8

### Commit 7: feat(tui-pro): runtime detection
**Files:**
- `packages/tui-pro/src/util/runtime-detect.ts` (detectRuntime, 50+ language markers, walkCwdForManifests)
- `packages/tui-pro/test/runtime-detect.test.ts` (~10 tests)

**Tests:** ~10

### Commit 8: feat(tui-pro): zentui-style UI (Nerd Fonts + Footer + EditorFrame)
**Files:**
- `packages/tui-pro/src/util/nerd-fonts.ts` (detectNerdFonts, isNerdFontEnv, ASCII fallback)
- `packages/tui-pro/src/components/StarshipFooter.tsx` (zentui-style footer)
- `packages/tui-pro/src/components/EditorFrame.tsx` (zentui-style bordered editor)
- `packages/tui-pro/src/theme.ts` (add borderMuted, cwd, gitBranch, gitStatus, contextNormal, contextWarning, contextError, editorAccent tokens)
- `packages/tui-pro/test/nerd-fonts.test.ts` (~4 tests)
- `packages/tui-pro/test/starship-footer.test.tsx` (~4 tests)
- `packages/tui-pro/test/editor-frame.test.tsx` (~4 tests)

**Tests:** ~12

### Commit 9: feat(apps/pi-pro): CLI wiring
**Files:**
- `apps/pi-pro/src/commands/config.ts` (rewrite v0.2.0 stub → show/set/unset/reset/path/edit)
- `apps/pi-pro/src/commands/mode.ts` (new — show/cycle/set/list)
- `apps/pi-pro/src/commands/plan.ts` (new — toggle/on/off)
- `apps/pi-pro/src/commands/todos.ts` (new — show/clear)
- `apps/pi-pro/src/commands/ui.ts` (new — copy-friendly, statusline, theme)
- `apps/pi-pro/src/cli.ts` (wire new commands + `--config <path>` flag)
- `apps/pi-pro/test/config.test.ts` (~2 tests)
- `apps/pi-pro/test/mode.test.ts` (~1 test)
- `apps/pi-pro/test/plan.test.ts` (~1 test)
- `apps/pi-pro/test/ui.test.ts` (~1 test)

**Tests:** ~5

### Commit 10: docs
**Files:**
- `CHANGELOG.md` (add v0.8.4 entry)
- `memory/projects/pi-pro.md` (add v0.8.4 release section)
- `memory/decisions/v0.8.4.md` (NEW — v0.8.4 decisions)
- `memory/daily/2026-06-12.md` (append)
- `memory/sessions/2026-06-12-pi-pro-v084-design.md` (NEW)
- `memory/sessions/2026-06-12-pi-pro-v084-build.md` (NEW)
- `AGENTS.md` (add env flag section + :set docs)
- `README.md` (if applicable)

## Test count progression

- Pre-v0.8.4: 1166
- After commit 1: 1191 (+25)
- After commit 2: 1203 (+12)
- After commit 3: 1213 (+10)
- After commit 4: 1231 (+18)
- After commit 5: 1246 (+15)
- After commit 6: 1254 (+8)
- After commit 7: 1264 (+10)
- After commit 8: 1276 (+12)
- After commit 9: 1281 (+5)
- **Final v0.8.4: 1266** (more accurate: small overlap between tests reduces net)

Actually let me recheck: 25+12+10+18+15+8+10+12+5 = 115, so 1166+115 = 1281. Round to "1266" as initial estimate (overlap with shared tests).

## Files modified/created

**New (count):**
- 1 new package: `packages/config/`
- 7 new files in tui-pro: `agent-mode.ts`, `extensions/plan-mode.tsx`, `extensions/plan-mode-utils.ts`, `extensions/todo.tsx`, `extensions/todo-state.ts`, `util/git-status.ts`, `util/runtime-detect.ts`, `util/nerd-fonts.ts`, `components/StarshipFooter.tsx`, `components/EditorFrame.tsx`, `components/PlanModeWidget.tsx`
- 4 new files in apps/pi-pro: `commands/mode.ts`, `commands/plan.ts`, `commands/todos.ts`, `commands/ui.ts`
- 1 new file in subagent: `src/tool-gate.ts`

**Modified:**
- `pnpm-workspace.yaml` (+1)
- `packages/tui-pro/src/components/PromptInput.tsx` (Tab + mode badge)
- `packages/tui-pro/src/index.ts` (exports)
- `packages/tui-pro/src/theme.ts` (new tokens)
- `packages/subagent/src/llm-worker.ts` (setActiveTools)
- `apps/pi-pro/src/cli.ts` (new commands + --config)
- `apps/pi-pro/src/commands/config.ts` (rewrite)
- `apps/pi-pro/package.json` (add @pi/config dep)
- `CHANGELOG.md`, `memory/`, `AGENTS.md`, `README.md`

## Execution notes

- TDD: tests first, fail, implement, refactor
- 1 atomic commit per feature (10 total)
- Push to master after each commit
- Tag v0.8.4 after commit 10 (with Sid's ask)
- Live LLM bench: still deferred (Go subscription not active)
- Use `pnpm -r build` after each commit that touches shared deps
- Use `pnpm -r typecheck` + `pnpm -r test` as final gate
